# Base Directives

**NO MOCKING. NO SIMULATED DATA. If a native API prevents direct data routing, you must write a WASM or low-level workaround to extract the raw byte buffers. Never fake the math. If you cannot access the raw data, state the exact architectural limitation.**
