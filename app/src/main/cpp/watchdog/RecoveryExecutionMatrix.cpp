#include "RecoveryExecutionMatrix.h"
#include <iostream>
void RecoveryExecutionMatrix::triggerRecovery(const std::string& anomaly) {
    std::cout << "[RecoveryExecutionMatrix] Triggering recovery for: " << anomaly << std::endl;
}
