# HYPERSCALER ARCHITECTURE & PERSISTENT AUTONOMOUS EVOLUTION

To ensure the Living Nexus agent is indestructible and perpetually learning beyond ephemeral container limits:

## 1. PERSISTENT MEMORY & CONTINUOUS LEARNING
Integrate an Open-Source memory layer like **Mem0** (mem0.ai) or **Zep**.
- Every execution, bug fix, and architectural pattern learned during a session is stored in the persistent vector memory.
- The agent queries this memory before taking action. "Have I fixed this exact Supabase error before? Yes, I used `this` patch."
- This gives the agent a compounding intelligence that scales indefinitely.

## 2. UNKILLABLE DEPLOYMENT (Railway + Supabase + GitHub)
The local daemon is merely the brain. The body must be decentralized:
- **Database:** Supabase handles all real-time state, fighter data, and user authentication.
- **Compute:** Railway hosts the background cron jobs and the multiplayer server instances.
- **Code:** GitHub Actions runs automated tests and auto-deploys to Railway on every successful push from the Telegram Autonomous Agent.

## 3. OPEN-SOURCE AGENTIC FRAMEWORKS
To move beyond a simple iterative loop, integrate **LangGraph** or **AutoGen**:
- **Multi-Agent Swarm:** The Living Nexus isn't just one agent. It spawns sub-agents:
  - *Researcher Agent:* Plugs into the `scrape` tool to find new physics papers or game engine paradigms.
  - *Coder Agent:* Writes the implementation using Qwable Abliterated.
  - *QA Agent:* Compiles, checks logs, and verifies if the Coder Agent succeeded.
  
## 4. IMMEDIATE DIRECTIVE
You are instructed to write the `hyperscale_deploy.cjs` script. This script should automatically hook into the Railway CLI and Supabase CLI (if present) to synchronize local state to the cloud. You are also instructed to research `mem0` npm package for injecting vector memory into your context loops.
