// Copyright BANNON.
#include "BannonSubmissionManager.h"

void UBannonSubmissionManager::InitiateSubmission(AActor* Attacker, AActor* Defender)
{
    StruggleValue = 50.0f;
    UE_LOG(LogTemp, Log, TEXT("Initiating submission struggle."));
}
