# Always needed (the API + stub backend run with just these — no GPU):
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2

# TOPOLOGY GUARD (post_process) + image-seed loader — CPU, cheap, install alongside any real backend
# so generated meshes are welded + decimated to a game-ready, physics-safe budget before rigging:
trimesh>=4.0
fast-simplification>=0.1     # quadric decimation backend trimesh routes to
numpy>=1.24
pillow>=10                   # image seed (sketch / concept sheet -> image->3D)

# --- GPU backends (install the set for your FORGE_BACKEND) ---
# Hunyuan3D-2 (FORGE_BACKEND=hunyuan): native text->3D + texture
#   pip install torch trimesh
#   pip install git+https://github.com/Tencent/Hunyuan3D-2.git   # provides hy3dgen
#
# TRELLIS (FORGE_BACKEND=trellis): image->3D, needs a text->image step (sdxl-turbo)
#   pip install torch diffusers trimesh
#   pip install git+https://github.com/microsoft/TRELLIS.git
#
# Auto-rig (FORGE_RIG=1): UniRig (VAST-AI/UniRig) — skeleton + skin weights
#   see https://github.com/VAST-AI-Research/UniRig  (wrap its inference as unirig.rig_glb)
