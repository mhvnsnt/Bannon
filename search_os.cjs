const axios = require('axios');
const fs = require('fs');
const path = require('path');

const GOOGLE_API_KEY = process.env.GOOGLE_API_KEY; // I will use the AI Studio tool instead since I can't guarantee API key presence.
